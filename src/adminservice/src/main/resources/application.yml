server:
  port: 8080

spring:
  application:
    name: adminservice

admin:
  api-key: ${ADMIN_API_KEY:dev-change-me}

management:
  endpoints:
    web:
      exposure:
        include: health,info

info:
  app:
    name: adminservice
    description: Internal admin API for Online Boutique
    version: 0.1.0-SNAPSHOT
